import { makeStyles } from "@mui/styles";

const useToolbarSchedularStyle = makeStyles({
  surface: {
    width: "100%",
    display: "flex",
    justifyContent: "space-between",
    padding: "10px",
    boxSizing: "border-box",
    gap: "30px",
  },
});

export default useToolbarSchedularStyle;
