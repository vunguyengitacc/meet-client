import { makeStyles } from "@mui/styles";

const useImageExportStyle = makeStyles({
  surface: {
    width: "100%",
    height: "100%",
    display: "flex",
  },
  form: {
    backgroundColor: "white",
    width: "600px",
    maxWidth: "80vw",
    borderRadius: "15px",
    padding: "5px",
    margin: "auto",
  },
});

export default useImageExportStyle;
