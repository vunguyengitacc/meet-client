import { makeStyles } from "@mui/styles";

const useWhiteBoardFeatureStyle = makeStyles({
  surface: {
    maxWidth: "100vw",
    maxHeight: "100vh",
    overflow: "hidden",
  },
});

export default useWhiteBoardFeatureStyle;
